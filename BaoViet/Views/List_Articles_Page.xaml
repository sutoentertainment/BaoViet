<Page
    x:Class="BaoViet.Views.List_Articles_Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BaoViet.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:model="using:BaoViet.Models">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <ListView Grid.Row="1" ItemContainerStyle="{StaticResource ListViewStandartItemStyle}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:FeedItem">
                    <Grid Margin="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        
                        <!--<Image Width="60" Height="60" Visibility="{Binding IsImageAvailable}"/>-->

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding ArticleTitle}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding ArticleDescription}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
